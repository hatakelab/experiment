name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Rustup
      run: rustup target add wasm32-unknown-unknown
    - name: Cargo new
      run: cargo new --lib skeleton
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: artifacts
        path: ./**
